const width = 18
const height = 10

// return true if the picture's width is greater then its height

const isLandscape = (width, height) => {

   return  (width > height) 
}

console.log(`[is landscape] ${isLandscape(width, height)}`)